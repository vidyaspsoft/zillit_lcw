<?xml version="1.0" encoding="utf-8"?>
<!-- Type Manager dialog — matches web's ScheduleTypeManager.
     Scrollable list of types, add custom type section, done button. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@color/surface"
    android:maxHeight="600dp">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@color/drawerHeaderBg"
        android:paddingHorizontal="@dimen/spacing_lg"
        android:paddingVertical="@dimen/spacing_base">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/tm_title"
            style="@style/ZillitTitle" />

        <ImageButton
            android:id="@+id/btnCloseTypes"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            android:contentDescription="@string/tm_done" />
    </LinearLayout>

    <View style="@style/ZillitDivider" />

    <!-- Scrollable types list -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/typesList"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingVertical="@dimen/spacing_sm" />

    <View style="@style/ZillitDivider" />

    <!-- Add Custom Type section (pinned at bottom) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/spacing_base"
        android:background="@color/surfaceAlt">

        <TextView
            style="@style/ZillitLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/tm_add_custom" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Color picker circle -->
            <View
                android:id="@+id/newTypeColor"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="@drawable/bg_today_circle"
                android:backgroundTint="@color/typePrep"
                android:layout_marginEnd="@dimen/spacing_sm" />

            <!-- Type name input -->
            <EditText
                android:id="@+id/etNewTypeName"
                style="@style/ZillitInput"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:hint="@string/tm_type_name_hint"
                android:inputType="text"
                android:layout_marginEnd="@dimen/spacing_sm" />

            <!-- Add button -->
            <Button
                android:id="@+id/btnAddType"
                style="@style/ZillitButton.Primary"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/tm_add" />
        </LinearLayout>

        <!-- Error message -->
        <TextView
            android:id="@+id/tvTypeError"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="@dimen/text_sm"
            android:textColor="@color/dangerBg"
            android:visibility="gone"
            android:layout_marginTop="@dimen/spacing_xs" />
    </LinearLayout>

    <View style="@style/ZillitDivider" />

    <!-- Done button -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="end"
        android:padding="@dimen/spacing_base">

        <Button
            android:id="@+id/btnDone"
            style="@style/ZillitButton.Primary"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/tm_done" />
    </LinearLayout>
</LinearLayout>

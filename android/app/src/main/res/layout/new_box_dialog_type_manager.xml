<?xml version="1.0" encoding="utf-8"?>
<!-- Schedule Types full-screen page (mirrors iOS TypeManagerView).
     Back arrow + centered title, scrollable list, pinned Add Custom Type footer. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/pageBg">

    <!-- Top bar: back arrow + centered title -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@color/surface"
        android:paddingHorizontal="@dimen/spacing_sm">

        <ImageButton
            android:id="@+id/btnCloseTypes"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_chevron_left"
            android:contentDescription="@string/tm_done"
            app:tint="@color/textPrimary" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/tm_title"
            android:textSize="@dimen/text_md"
            android:textStyle="bold"
            android:textColor="@color/textPrimary"
            android:gravity="center" />

        <!-- Spacer to balance back arrow so the title is truly centered -->
        <Space android:layout_width="40dp" android:layout_height="40dp" />
    </LinearLayout>

    <View style="@style/ZillitDivider" />

    <!-- Scrollable types list -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/typesList"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@color/pageBg"
        android:clipToPadding="false" />

    <View style="@style/ZillitDivider" />

    <!-- Add Custom Type footer (pinned at bottom, iOS parity) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/spacing_base"
        android:background="@color/surface">

        <TextView
            style="@style/ZillitLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/tm_add_custom"
            android:layout_marginBottom="@dimen/spacing_sm" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Color picker circle -->
            <View
                android:id="@+id/newTypeColor"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:background="@drawable/bg_today_circle"
                android:backgroundTint="@color/typePrep"
                android:layout_marginEnd="@dimen/spacing_sm" />

            <!-- Type name input -->
            <EditText
                android:id="@+id/etNewTypeName"
                style="@style/ZillitInput"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:hint="@string/tm_type_name_hint"
                android:inputType="text"
                android:layout_marginEnd="@dimen/spacing_sm" />

            <!-- + Add button (outlined, primary accent — matches iOS) -->
            <TextView
                android:id="@+id/btnAddType"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="+ Add"
                android:textSize="@dimen/text_body"
                android:textColor="@color/primaryAccent"
                android:textStyle="bold"
                android:paddingHorizontal="@dimen/spacing_md"
                android:paddingVertical="@dimen/spacing_sm"
                android:background="@drawable/bg_button_primary_soft" />
        </LinearLayout>

        <!-- Error message -->
        <TextView
            android:id="@+id/tvTypeError"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="@dimen/text_sm"
            android:textColor="@color/dangerBg"
            android:visibility="gone"
            android:layout_marginTop="@dimen/spacing_xs" />

        <!-- Hidden legacy Done button kept so ViewBinding compiles -->
        <TextView
            android:id="@+id/btnDone"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:visibility="gone" />
    </LinearLayout>
</LinearLayout>
